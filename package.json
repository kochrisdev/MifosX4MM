{
  "name": "mifos-x",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev": "dotenv -e .env -- turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "docker:up": "docker compose up -d --build",
    "docker:down": "docker compose down",
    "seed": "tsx scripts/seed-fineract.ts"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "dotenv-cli": "^11.0.0",
    "turbo": "^2.0.0",
    "typescript": "^5.4.0"
  }
}
